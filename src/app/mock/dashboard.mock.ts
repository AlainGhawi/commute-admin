import { type DashboardStats } from '../features/rides/models/ride.model';

export const MOCK_DASHBOARD_STATS: DashboardStats = {
  totalEmployees: 342,
  activeRides: 28,
  matchRate: 0.73,
  co2SavedKg: 1250,
};
